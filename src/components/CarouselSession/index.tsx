import NavBar from '../NavBar';

export default function CarouselSession() {
	return (
		<section className='w-full h-3/4 bg-slate-50 flex flex-col items-center'>
			<NavBar />

			<span>carousel session</span>
		</section>
	);
}
